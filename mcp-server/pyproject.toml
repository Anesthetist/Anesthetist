[project]
name = "srl-vault-mcp"
version = "0.1.0"
description = "MCP server for querying the Somnistics Research Labs knowledge vault"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "pyyaml>=6.0",
    "python-frontmatter>=1.0",
]
